/** @format */

import {AppRegistry} from 'react-native';
import AppTabsNavigator from './Tabs';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppTabsNavigator);
